# 思考思路

## 2026-08-06 设计记录

### 第 1 次总结

#### 思考过程

用户希望设计一个“工作找房” App：以工作地点为起点，结合地图、交通和出租信息，帮助用户选择居住位置。

经过三轮需求确认，产品的主要使用者确定为刚入职或换工作、正在找房的上班族。此类用户不是只想浏览房源，而是希望同时解决通勤时间、租金预算、区域陌生、房源可信度和生活配套等问题。因此产品定位应从“房源地图”升级为“围绕工作地点做居住决策的综合助手”。

用户偏好的初始流程是：先输入公司地址，再填写或调整预算、可接受通勤时间、交通方式等条件，最后在地图上查看推荐区域和房源。这样既保留地图探索的自由度，也能让系统先过滤出更符合实际需求的结果。

#### 当前进度

- 已确定目标用户：刚入职或换工作的上班族。
- 已确定核心价值：在租金、通勤和居住条件之间进行综合权衡。
- 已确定基本入口：输入工作地点。
- 已确定交互方向：条件设置与地图浏览结合。
- 尚未确定：第一版必须支持的功能、推荐结果的排序方式、房源数据来源及产品形态。

#### 下一步

继续确认用户最希望看到的第一屏结果，并据此收敛 MVP（最小可用版本）的功能范围。

### 第 2 次总结

#### 思考过程

第一屏确定为“地图 + 房源列表”的综合结果页。用户输入工作地点后，可以设置最长通勤时间，也可以切换为“某个时间前到达公司”的目标到达模式；同时选择地铁、公交、驾车、骑行或步行等通勤方式。

讨论进一步明确了产品的核心差异：通勤推荐不能只追求最短时间。有些路线可能只快 10 分钟，却需要多次倒车；用户可能更愿意选择稍慢但更稳定、更省心的路线。因此房源排序需要同时考虑总时间、换乘次数、步行时间、交通费用和路线复杂度，并展示“最快”“少换乘”“最省钱”等可理解的标签。

用户希望自己调节“时间优先 / 舒适优先”的权重，而不是由系统固定判断。由此可以设计一个简单的偏好滑杆或快捷选项，让用户在不同工作日和生活阶段采用不同的通勤策略。

#### 当前进度

- 已确定结果页：地图与房源列表联动。
- 已确定筛选：预算、通勤时间、目标到达时间、通勤方式。
- 已确定推荐逻辑：同时考虑时间和通勤舒适度。
- 已确定个性化方向：用户可调节时间与舒适度权重。
- 尚未确定：房源信息字段、数据来源、推荐评分展示方式、第一版是否接入真实房源平台。

#### 下一步

确认房源详情中用户最需要比较的信息，再设计第一版 MVP 的页面和数据结构。

### 第 3 次总结

#### 思考过程

产品阶段确定为“先做一个可以放到 GitHub 并在线访问的产品”。因此采用移动端优先的 React/Vite/TypeScript Web App，先用本地演示数据验证完整体验，再为真实数据接口预留替换位置。相较于直接做原生 App，这个方案可以通过 GitHub Pages 快速发布，也更方便用户在电脑和手机浏览器中体验。

第一版正式收敛为一个“地图 + 房源列表”的工作找房页面：左侧设置工作地点、预算、最长通勤时间或目标到达时间、交通方式和时间/舒适度权重；右侧地图与房源卡片联动。推荐结果会展示租金、户型、面积、地铁距离、通勤时间、换乘次数和推荐标签，并允许收藏和加入对比。

实现时把推荐评分独立为纯函数：预算匹配度、时间匹配度、换乘次数、步行时间和路线可靠性共同影响结果。这样未来改变“少换乘优先”的产品策略时，只需调整评分逻辑。地图使用 Leaflet + OpenStreetMap，部署不需要后端服务器；GitHub Actions 已配置为推送到 main 分支后自动构建并发布 GitHub Pages。

#### 当前进度

- 已完成 React/Vite/TypeScript 项目骨架和依赖安装。
- 已完成响应式产品界面、地图、房源列表、筛选面板和详情条。
- 已完成租金、通勤时间、到达时间、交通方式和偏好滑杆交互。
- 已完成本地收藏、最多 3 套房源对比和提示反馈。
- 已完成 GitHub Pages 自动部署工作流与 README。
- 已通过生产构建检查和浏览器交互检查。

#### 下一步

继续完善正式上线前的项目配置，例如补充图标、进一步拆分测试，并在 GitHub 仓库中启用 Pages 的 GitHub Actions 来源。

### 第 4 次总结

#### 记录时间

2026-08-08 18:23:48（Asia/Shanghai）

#### 思考过程

用户希望把项目从 C 盘迁移到 E 盘，并删除 C 盘文件。为避免跨盘移动过程中发生文件丢失，先将整个项目复制到 `E:\Codex\Projects\住得近`，然后逐项比较源目录与目标目录的文件相对路径、文件大小，并额外核对核心源码、依赖锁文件和本文档的 SHA-256 哈希。全部验证一致后，才清理 C 盘内容。

#### 当前进度

- E 盘项目迁移完成，共 6,397 个文件、122,442,042 字节。
- 迁移前路径与大小差异为 0，核心文件哈希全部一致。
- C 盘原项目目录内的文件和子目录已全部删除。
- C 盘只剩被当前 Codex 任务占用的空目录，关闭或切换工作区后即可删除。
- 已在 E 盘重新执行 `npm run build`，生产构建成功。

#### 下一步

以后以 `E:\Codex\Projects\住得近` 作为唯一项目路径。在 Codex 中重新打开该目录后，删除 C 盘遗留的空目录。

### 第 5 次总结

#### 记录时间

2026-08-09 20:23:40（Asia/Shanghai）

#### 思考过程

本阶段将项目从“本地可运行”推进到“GitHub 可协作、网页可访问”。先配置 Git 作者姓名和提交邮箱，确认身份后完成第一次本地提交；随后创建公开的 GitHub 仓库，通过 HTTPS 远程地址建立 `origin`，并把本地 `main` 分支推送到远程。

第一次 GitHub Actions 运行中，构建步骤成功，但部署步骤返回 404。查看运行日志后确认原因不是代码错误，而是仓库级 GitHub Pages 尚未启用。将仓库 `Settings → Pages → Source` 设置为 `GitHub Actions` 后重新运行任务，构建和部署均成功。

#### 当前进度

- 已配置 Git 提交身份。
- 已完成第一次本地提交，提交编号以 `5bd367a` 开头。
- 已上传到公开仓库 `zeyuan-ai/zhudejin`。
- 本地 `main` 已跟踪远程 `origin/main`。
- GitHub Actions 的 build 和 deploy 均已通过。
- 线上地址 `https://zeyuan-ai.github.io/zhudejin/` 返回 HTTP 200，页面标题正确。

#### 下一步

使用本次新增的记录练习一次标准更新流程：查看修改、创建功能或文档分支、提交、推送、创建 Pull Request，并观察合并到 `main` 后的自动部署。

### 第 6 次总结

#### 记录时间

2026-08-23（Asia/Shanghai）

#### 思考过程

本阶段把产品从可浏览的界面原型推进为可验证的 MVP。核心定位进一步明确：高德和百度负责地图与路线计算，“住得近”负责把多套房源、硬筛选条件和个人通勤偏好组合为可解释的租房决策。产品不宣称路线算法优于地图平台，也不直接开展房源交易。

实施采用渐进增强：配置高德和 Supabase 后使用真实地图与服务端数据；尚未配置时继续提供有明显标识的演示模式。这样既能保持 GitHub Pages 随时可预览，也能避免把服务端密钥提交到公开仓库。所有预算、面积和租赁类型等硬条件都必须严格执行，删除原型中自动放宽条件的行为。

#### 当前进度

- 已确认并锁定完整 MVP 实施计划。
- 已建立 `feat/productize-mvp` 功能分支。
- 已完成现有代码、部署工作流和依赖安全状态检查。
- 已开始编写 PRD、技术架构和环境变量模板。

#### 下一步

实现扩展后的筛选与评分模型、高德地图适配、完整结果状态、Supabase 数据库与 Edge Functions，并用自动化测试验证核心决策场景。

### 第 7 次总结

#### 记录时间

2026-08-25 21:50:20（Asia/Shanghai）

#### 思考过程

最近三轮从“代码实施”推进到“可以运行和清楚汇报”。第一轮完成一键启动和本地服务，解决入门用户不知道如何打开项目的问题；第二轮根据实际页面反馈补齐推荐逻辑、产品说明、真实地理演示底图和 API 配置入口，并修复移动端导航；第三轮重新审查需求与代码，发现必须区分“代码已经写出”和“真实服务已经上线”。

因此需求管理改用双进度口径：开发实现完成度用于衡量界面、代码、文档和测试；上线准备度用于衡量 API 账号、合法房源、后端部署、性能样本和真实用户反馈。这样可以避免把未部署的后端骨架或演示数据误判为已完成产品。

#### 当前进度

- 开发实现加权完成度约 83%。
- 上线准备度约 46%，但真实后端和房源尚未上线。
- 已完成真实 OpenStreetMap 演示底图、高德 JS Key 本机配置入口、推荐逻辑与产品说明页面。
- 已完成 4 个单元测试、6 个桌面/移动端测试、生产构建和密钥扫描。
- Supabase、高德 Web 服务和百度核验代码已存在，但尚未配置真实账号和部署。
- 当前仍使用演示房源，尚未形成可运营的真实房源库。

#### 下一步

按 PRD 中的 P0 清单先创建 Supabase 项目、配置高德 Key、导入第一批合法真实房源并完成真实全链路测试，然后再提交推送到 GitHub。

### 第 8 次总结

#### 记录时间

2026-08-25 23:24:30（Asia/Shanghai）

#### 思考过程

最近三轮正式开始 Supabase 上线准备。先将 Supabase CLI 作为项目开发依赖安装，避免依赖不稳定的全局环境；npm 缓存和项目依赖继续保存在 E 盘。随后排查了 Codex 自动终端无法进行交互式登录的问题，确认这是运行环境强制 JSON 输出导致的交互限制，因此改为由用户在 VS Code 终端完成一次浏览器授权，后续数据库与函数部署仍由项目命令管理。

登录阶段允许使用 GitHub 账号完成 Supabase 身份验证，但需要区分“使用 GitHub 登录 Supabase”和“让 Supabase 连接代码仓库”：前者只验证开发者身份，后者是后续可选的仓库集成功能。一次性 CLI 验证码属于临时敏感信息，必须直接输入本机终端，不应截图或提交到聊天、文档和 Git 仓库。

#### 当前进度

- 已在项目中安装并验证 Supabase CLI 2.115.0。
- `package.json` 与 `package-lock.json` 已更新，npm 缓存继续使用 E 盘目录。
- 已启动 Supabase CLI 浏览器登录流程。
- 已确认可使用 GitHub 账号授权 Supabase。
- 尚未完成 CLI 登录、云端项目创建、本地关联和数据库迁移。
- 本轮没有把 Supabase 密钥、访问令牌或数据库密码写入项目。

#### 下一步

重新生成未公开的一次性验证码并在本机终端完成登录。登录成功后读取组织信息、创建云端项目，再依次执行本地项目关联、数据库迁移、Edge Functions 部署和前端环境配置。

### 第 9 次总结

#### 记录时间

2026-08-25 23:46:01（Asia/Shanghai）

#### 思考过程

最近三轮完成了 Supabase 身份登录、数据区域风险判断和国内生产迁移方向确认。CLI 已能访问账号，并创建“住得近”组织。考虑到 Supabase 托管项目在新加坡，必须区分“安全存储”和“数据出境”：加密与权限控制可以降低泄露风险，但工作地点、路线起终点和搜索偏好若被传送到新加坡，仍属于境外处理。

产品当前只做少量已知人员内测，因此接受新加坡 Supabase 作为临时验证环境，同时收紧数据边界：不收集直接身份信息，不长期记录精确工作地点，缓存和搜索日志按期限清理。正式面向中国用户时，不选择长期自建整套 Supabase，而迁移到阿里云上海的 RDS PostgreSQL 与函数计算，降低运维难度并让用户请求在境内处理。

#### 当前进度

- Supabase CLI 登录完成。
- 已创建 Supabase 组织“住得近”。
- 已确认新加坡 Supabase 只承担少量人员内测。
- 已确认国内生产目标为阿里云上海 RDS PostgreSQL + 函数计算。
- 已建立独立迁移设计，并同步 PRD 与技术架构的数据边界。
- Supabase 云端数据库项目尚未创建，数据库迁移和函数部署尚未执行。

#### 下一步

在新加坡区域创建 `zhudejin-staging` 内测项目，由用户在本机安全输入数据库密码。随后关联本地项目、执行 migration、配置最小化日志策略并部署 Edge Functions。代码侧增加统一 API 基础地址和平台适配边界，为以后迁入阿里云上海做准备。
